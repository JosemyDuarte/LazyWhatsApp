---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class={`p-6 rounded-lg bg-card border border-card-border shadow-[0_4px_24px_rgba(0,0,0,0.2)] transition-all duration-200 ease-bounce hover:-translate-y-1 hover:border-primary/30 backdrop-blur-md ${className || ''}`}>
	<slot />
</div>
